# v4: Tools 补齐（List/Bash/WebSearch/NotebookEdit/lsp）

## Checklist

- [ ] `List`：ignore + limit + tree output
- [ ] `Bash`：bash/sh + timeout + output truncation + full output file（可选）
- [ ] `WebSearch`：Tavily + DDG fallback + domain filters
- [ ] `NotebookEdit`：insert/replace/delete + source normalize
- [ ] `lsp`：OpenCode config + stdio client + operations
- [ ] 单测覆盖（至少每个工具 2~3 个）

